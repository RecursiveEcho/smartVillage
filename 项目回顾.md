# SmartVillages 项目回顾

## 一、项目定位

SmartVillages 是一个智慧乡村管理系统，目标不是只做 CRUD，而是打磨成一个实习和面试都能讲清楚的大项目。

核心目标：

- 有完整业务闭环：公开浏览、村民提交、村干部审核处理、管理员管理。
- 有工程化亮点：缓存、限流、TraceId、统一异常、事件驱动、模块解耦。
- 有面试表达：每个亮点都能讲清“问题 -> 方案 -> 为什么这么做 -> 如何验证”。

项目结构：

```text
SmartVillages/
  SmartVillages-Backend/      Spring Boot 3.x + Java 17 多模块后端
    service/                  启动入口模块
    common/                   公共能力：过滤器、异常、结果、缓存、事件
    auth/                     认证与用户
    admin/                    管理员
    announcement/             公告
    feature/                  乡村风采
    interaction/              留言互动
    media/                    媒体资源
    management/               村务管理
  SmartVillages-Web/          Vue 3 + Vite 前端
```

## 二、请求处理流程

后端请求的大体链路：

```text
前端请求
  -> TraceIdFilter
  -> JwtSecurityFilter
  -> Spring Security
  -> Controller
  -> Service
  -> Mapper / Redis / OSS
  -> Result<T> 统一响应
```

关键点：

- `TraceIdFilter` 负责生成或读取 `X-Trace-Id`，写入 MDC，并在响应头返回。
- `JwtSecurityFilter` 从请求头 `token` 解析登录用户信息。
- `LoginUserContext` 从 request 属性里读取 `authId`、`username`、`role`。
- `GlobalExceptionHandler` 统一处理业务异常、权限异常、限流异常、文件过大异常等。
- `Result<T>` 统一返回格式，前端按 `code/message/data` 处理。

面试讲法：

- 问题：接口多了以后，日志追踪、认证、异常返回如果各写各的，会很难排查问题。
- 方案：把横切逻辑放到过滤器和全局异常处理器里。
- 为什么这样做：Controller 只关心业务逻辑，公共逻辑统一收口。
- 如何验证：请求接口时检查响应头 `X-Trace-Id`，同时查看日志里是否能按同一个 traceId 串起来。

## 三、认证与权限流程

登录流程：

```text
POST /auth/login
  -> 登录限流校验
  -> 查询用户
  -> 校验密码
  -> 生成 JWT
  -> 返回 token
```

请求认证流程：

```text
前端携带 token
  -> JwtSecurityFilter 解析 token
  -> 写入 request 属性
  -> 写入 Spring Security 上下文
  -> Controller / Service 通过 LoginUserContext 取当前用户
```

角色划分：

| 角色 | 说明 |
|------|------|
| `ROLE_VILLAGER` | 村民，提交留言、工单、查看公开信息 |
| `ROLE_CADRE` | 村干部，管理公告、风采、媒体、村务 |
| `ROLE_ADMIN` | 系统管理员，管理用户 |

登录限流亮点：

- Redis 按 `IP + 用户名` 计数。
- 超过阈值抛出 `RATE_LIMIT_EXCEEDED`。
- 全局异常处理器把它映射成 429。

面试讲法：

- 问题：登录接口容易被暴力尝试。
- 方案：用 Redis 做短时间窗口计数，按 IP 和用户名组合限流。
- 为什么不用本地 Map：多实例部署时本地内存不共享，Redis 更适合做统一计数。
- 如何验证：连续多次错误登录，超过阈值后返回 429。

## 四、微服务化改造

这一阶段项目的主线，不再只是继续堆业务接口，而是把后端从“单体思路”往“可讲清楚的微服务项目”推进。

当前后端已经拆成 5 个核心服务：

| 服务 | 职责 | 端口 |
|------|------|------|
| `gateway-service` | 统一入口、路由转发 | `8090` |
| `auth-service` | 登录认证、用户身份、用户名查询 | `8082` |
| `business-service` | 公告、乡村风采、留言互动、村务等核心业务 | `8081` |
| `media-service` | 媒体上传、审核、资源绑定 | `8083` |
| `admin-service` | 管理端用户管理，内部通过 Feign 调 auth-service | `8084` |

拆分后的调用关系：

```text
前端请求
  -> gateway-service
  -> 按路径转发到 auth / business / media

business-service / media-service
  -> 需要用户名时通过 Feign 调 auth-service

media-service
  -> 审核通过后通过 RabbitMQ 发送绑定消息
  -> business-service / auth-service 消费并执行绑定
```

这次微服务化改造主要完成了四块能力：

### 4.1 Gateway 统一入口

- 新增 `gateway-service` 作为统一入口。
- 外部不再直接请求每个服务，而是先走网关。
- 路由配置里已经改成按服务名转发，而不是写死固定地址。

关键文件：

- `SmartVillages-Backend/gateway-service/src/main/resources/application.yml`
- `SmartVillages-Backend/gateway-service/src/main/java/com/backend/gateway/GatewayServiceApplication.java`

当前典型路由：

- `/auth/**` -> `lb://auth-service`
- `/admin/**` -> `lb://admin-service`
- `/api/**` -> `lb://business-service`
- `/media/**` -> `lb://media-service`
- `/announcements/**` -> `lb://business-service`
- `/interactions/**` -> `lb://business-service`
- `/cadre/**` -> `lb://business-service`
- `/villager/**` -> `lb://business-service`
- `/public/**` -> `lb://business-service`
- `/features/**` -> `lb://business-service`

补充说明：

- 原本网关默认端口是 `8080`
- 因为本地 Nacos 3.2.2 控制台占用了 `8080`
- 最后把网关改到 `8090`

面试讲法：

- 问题：前端如果直接对接多个后端服务，入口分散，后续鉴权、限流、统一路由都会很乱。
- 方案：引入 `gateway-service` 作为统一入口。
- 为什么这样做：把“入口层能力”和“业务层能力”分开，后面加限流、统一鉴权、统一日志都更自然。
- 如何验证：访问网关路径，确认请求能被正确转发到对应服务。

### 4.2 Nacos 注册中心

这一步解决的是：服务之间不再写死地址。

改造前：

```text
business-service 想找 auth-service
  -> 代码里写 localhost:8082
```

改造后：

```text
business-service 想找 auth-service
  -> 只写服务名 auth-service
  -> 先去 Nacos 查地址
  -> 再发请求
```

这次已经完成：

- 五个服务都接入了 Nacos Discovery
- 五个启动类都开启了服务发现能力
- 五个服务都成功注册进 Nacos

关键点：

- `spring-cloud-starter-alibaba-nacos-discovery`
- `@EnableDiscoveryClient`
- `spring.application.name`
- `spring.cloud.nacos.discovery.server-addr`

关键文件：

- `SmartVillages-Backend/business-service/src/main/java/com/backend/business/BusinessServiceApplication.java`
- `SmartVillages-Backend/auth-service/src/main/java/com/backend/authservice/AuthServiceApplication.java`
- `SmartVillages-Backend/media-service/src/main/java/com/backend/mediaservice/MediaServiceApplication.java`
- `SmartVillages-Backend/admin-service/src/main/java/com/backend/adminservice/AdminServiceApplication.java`
- `SmartVillages-Backend/gateway-service/src/main/java/com/backend/gateway/GatewayServiceApplication.java`

验证结果：

- Nacos 服务列表里可以看到：
  - `auth-service`
  - `business-service`
  - `media-service`
  - `admin-service`
  - `gateway-service`
- 每个服务都存在 1 个健康实例

面试讲法：

- 问题：微服务一旦拆开，服务地址不能继续写死，否则后面扩容、迁移地址、换环境都会很麻烦。
- 方案：引入 Nacos 作为注册中心，让服务启动时自动注册，调用方按服务名发现目标地址。
- 为什么这样做：服务调用从“固定地址耦合”变成“服务名解耦”。
- 如何验证：在 Nacos 控制台确认服务注册成功，同时通过服务名调用验证链路可用。

### 4.3 Feign 同步调用

这一步解决的是：业务服务不再直接依赖认证服务的表，而是通过 HTTP 调用认证服务。

本次最典型的改造是“用户名回显”：

```text
business-service / media-service
  -> 需要用户名映射
  -> 调 auth-service 的 /internal/auth/usernames
  -> auth-service 作为用户名数据拥有者统一返回
```

关键点：

- `auth-service` 负责用户名数据
- `business-service` 和 `media-service` 不再直接碰认证表
- 同步调用从固定地址改成了 Feign + 服务名调用

关键文件：

- `SmartVillages-Backend/business-service/src/main/java/com/backend/business/client/AuthUserClient.java`
- `SmartVillages-Backend/media-service/src/main/java/com/backend/mediaservice/client/AuthUserClient.java`
- `SmartVillages-Backend/auth-service/src/main/java/com/backend/authservice/controller/AuthInternalController.java`

面试讲法：

- 问题：拆成微服务后，如果业务服务继续直接查认证表，就只是“物理拆模块”，不是“职责真正拆开”。
- 方案：让 `auth-service` 成为用户名数据拥有者，对外提供内部查询接口；其他服务通过 Feign 调它。
- 为什么这样做：数据归属更清楚，后续认证服务独立演进时，业务服务不用再关心底层表结构。
- 如何验证：通过 `business-service -> auth-service` 和 `media-service -> auth-service` 的调用链，确认用户名回显仍然正确。

### 4.5 admin-service 拆分与 Feign 改造

这一步解决的是：管理员能力不再继续挂在原来的认证模块思路里，而是开始独立成服务。

本次已经完成：

- 新增 `admin-service`
- 网关增加 `/admin/** -> lb://admin-service`
- `admin-service` 成功注册到 Nacos
- 管理员用户管理从“直接查 auth 表”改成“Feign 调 auth-service 内部接口”

拆分后的调用关系：

```text
前端
  -> gateway-service
  -> /admin/**
  -> admin-service
  -> Feign 调 auth-service
```

本次内部接口主要包括：

- `/internal/auth/users`
- `/internal/auth/users/{id}`
- `/internal/auth/users/{id}/status`
- `/internal/auth/users/cadre`
- `/internal/auth/users/{id}`

关键文件：

- `SmartVillages-Backend/admin-service/src/main/java/com/backend/adminservice/AdminServiceApplication.java`
- `SmartVillages-Backend/admin/src/main/java/com/backend/admin/client/AuthAdminClient.java`
- `SmartVillages-Backend/admin/src/main/java/com/backend/admin/service/impl/AdminServiceImpl.java`
- `SmartVillages-Backend/auth-service/src/main/java/com/backend/authservice/controller/AuthInternalController.java`

设计重点：

- `auth-service` 继续作为用户数据拥有者
- `admin-service` 负责管理端流程编排
- Redis 缓存、分布式锁、删缓存、列表版本号仍然保留在 `admin` 侧
- 数据库读写权从 `admin` 收回到 `auth`

面试讲法：

- 问题：如果管理员模块继续直接依赖 `AuthMapper` 和认证表，那其实没有真正完成服务边界拆分。
- 方案：新增 `admin-service`，并把用户分页、详情、改状态、创建干部、删除用户都改成 Feign 调 `auth-service` 内部接口。
- 为什么这样做：用户数据归属保持在认证服务，管理端只负责流程和缓存策略，职责更清楚。
- 如何验证：`admin-service` 单独启动成功、注册进 Nacos，且通过网关访问 `/admin/users` 仍然能正常返回数据。

### 4.6 前端统一走 gateway-service

这一步解决的是：前端不再分别直连多个服务端口，而是统一从网关进入。

本次已经完成：

- 前端默认 API 入口改成 `8090`
- Vite 开发代理目标改成 `8090`
- 网关补齐业务服务前缀映射

当前前端统一入口：

```text
http://127.0.0.1:8090
```

前端现在统一通过这些前缀访问后端：

- `/auth/**`
- `/admin/**`
- `/media/**`
- `/api/**`
- `/announcements/**`
- `/interactions/**`
- `/cadre/**`
- `/villager/**`
- `/public/**`
- `/features/**`

面试讲法：

- 问题：如果前端直接连 `8081/8082/8083/8084`，入口分散，后续路由、鉴权、限流和联调都会越来越乱。
- 方案：把前端请求统一收敛到 `gateway-service`。
- 为什么这样做：前端只关心业务路径，不再关心服务实例和具体端口，后端入口也更符合微服务项目的真实结构。
- 如何验证：前端开发环境通过代理请求网关，页面上的 `/admin`、`/auth`、`/media`、`/announcements` 等请求都不再直连单个服务端口。

### 4.4 Nacos 配置中心

这一步解决的是：不只是“服务在哪”交给 Nacos，连一部分业务配置也开始集中管理。

当前已经完成：

- `business-service` 接入 Nacos Config
- `auth-service` 接入 Nacos Config
- `media-service` 接入 Nacos Config

关键点：

- 通过 `spring-cloud-starter-alibaba-nacos-config` 引入配置中心能力
- 通过 `spring.config.import` 读取 `optional:nacos:${spring.application.name}.yaml`
- 各服务在 Nacos 中维护各自的 `xxx-service.yaml`

已经验证通过的能力：

- `business-service` 能从 Nacos 读取 `custom.message`
- `auth-service` 已把 `jwt.expiration-ms` 迁进 Nacos
- `jwt.secret` 这类敏感配置仍然保留在本地

当前配置归类原则：

适合进 Nacos：

- JWT 过期时间
- 上传大小限制
- 业务开关
- 非敏感业务参数

先保留在本地 `.env` / `application.yml`：

- MySQL 密码
- Redis 密码
- RabbitMQ 密码
- OSS 密钥
- JWT Secret

面试讲法：

- 问题：微服务一多，配置散落在各服务本地文件里，修改和统一管理都会越来越麻烦。
- 方案：把非敏感、易变的业务配置逐步迁到 Nacos Config。
- 为什么不是一上来把所有配置都迁进去：敏感配置和底层连接配置需要更谨慎，先把适合集中管理的业务配置迁移，风险更可控。
- 如何验证：修改 Nacos 配置后重启服务，确认业务读取到的是 Nacos 中的值而不是本地默认值。

## 五、媒体上传、审核、绑定流程

这是目前项目里比较适合讲的完整业务闭环。

```text
用户上传媒体
  -> MediaServiceImpl.upload()
  -> 上传到 OSS
  -> media 表记录待审核数据
  -> 返回 UploadVO
  -> 村干部审核媒体
  -> 审核通过后发送 MediaBindMessage 到 RabbitMQ
  -> MediaBindMqConsumer 消费消息
  -> 根据 bindTarget 找到对应 MediaBinder
  -> 具体业务模块完成绑定
```

关键文件：

| 职责 | 文件 |
|------|------|
| 上传和审核入口 | `SmartVillages-Backend/media/src/main/java/com/backend/media/controller/MediaController.java` |
| 上传、审核、消息发送 | `SmartVillages-Backend/media/src/main/java/com/backend/media/service/impl/MediaServiceImpl.java` |
| OSS 上传 | `SmartVillages-Backend/media/src/main/java/com/backend/media/tool/OssUploadTool.java` |
| MQ 生产者 exchange 配置 | `SmartVillages-Backend/media-service/src/main/java/com/backend/mediaservice/config/MediaBindRabbitMqConfig.java` |
| business 队列配置 | `SmartVillages-Backend/business-service/src/main/java/com/backend/business/config/BusinessMediaBindRabbitMqConfig.java` |
| auth 队列配置 | `SmartVillages-Backend/auth-service/src/main/java/com/backend/authservice/config/AuthMediaBindRabbitMqConfig.java` |
| MQ 名称契约 | `SmartVillages-Backend/common-lib/src/main/java/com/backend/common/mq/MediaBindMqNames.java` |
| 绑定消息 | `SmartVillages-Backend/common-lib/src/main/java/com/backend/common/event/MediaBindMessage.java` |
| Binder 接口 | `SmartVillages-Backend/common-lib/src/main/java/com/backend/common/binder/MediaBinder.java` |
| business MQ 消费者 | `SmartVillages-Backend/business-service/src/main/java/com/backend/business/mq/BusinessMediaBindMqConsumer.java` |
| auth MQ 消费者 | `SmartVillages-Backend/auth-service/src/main/java/com/backend/authservice/mq/AuthMediaBindMqConsumer.java` |
| 风采绑定 | `SmartVillages-Backend/feature/src/main/java/com/backend/feature/binder/FeatureMediaBinder.java` |
| 公告绑定 | `SmartVillages-Backend/announcement/src/main/java/com/backend/announcement/binder/AnnouncementMediaBinder.java` |
| 头像绑定 | `SmartVillages-Backend/auth/src/main/java/com/backend/auth/binder/AuthMediaBinder.java` |

设计重点：

- 上传不等于绑定，上传后先进入审核流程。
- 审核通过后才触发绑定，避免未审核资源出现在业务实体中。
- `MediaServiceImpl.auditMedia()` 只负责审核主流程和发送消息，不直接写入公告、风采、头像等业务模块。
- `BusinessMediaBindMqConsumer` 和 `AuthMediaBindMqConsumer` 分别在各自服务内收集 `MediaBinder`，按 `bindTarget` 分发。
- 新增绑定目标时，新增一个 Binder 实现即可。
- 风采相册 `IMAGES_APPEND` 属于追加型写入，消费侧在追加前先判断图片 URL 是否已存在，避免 RabbitMQ 重复消费时把同一张图重复追加。
- MQ 队列名、交换机名、路由键放到 `common-lib` 的 `MediaBindMqNames`，作为跨服务共享契约。
- `media-service` 作为生产者，只保留 exchange 配置；`business-service` 和 `auth-service` 作为消费者，各自声明自己的 queue、binding、死信交换机和死信队列。这样队列归属和服务职责是一致的。
- 消费者按异常类型区分处理：`BusinessException` 说明消息本身有问题，不再重试，直接抛 `AmqpRejectAndDontRequeueException` 进入死信队列；其他异常保留重试，给数据库抖动、网络波动这类临时故障恢复机会。
- 死信队列不是“多此一举”，而是给无效消息一个出口。否则像“绑定到已删除风采”这种消息会在主队列里无限重试，持续占用消费者。

面试讲法：

- 问题：一个上传功能要服务头像、公告、乡村风采等多个业务场景。
- 方案：上传成功后保存绑定参数，审核通过后发送 RabbitMQ 消息，再由各服务内的 MQ 消费者按 `bindTarget` 找到对应 Binder 处理具体绑定。
- 为什么不在上传模块写大量 `if/else`：这样会让媒体模块直接依赖多个业务模块，后续扩展目标会越来越难维护；改成 MQ 后主流程也更轻。
- 如何验证：上传媒体时传不同 `bindTarget`，审核通过后检查对应业务实体字段是否被正确写入；对乡村风采相册重复投递同一条绑定消息，确认同一张图片不会被重复追加。

本轮验证结果：

- `AUTH + AVATAR` 已跑通：审核通过后，`auth-service` 成功消费消息并回写用户头像。
- `FEATURE + COVER` 已跑通：审核通过后，`feature.cover` 被新图片地址覆盖。
- `FEATURE + VIDEO` 已跑通：审核通过后，`feature.video` 被新视频地址覆盖。
- `FEATURE + IMAGES_APPEND` 已跑通：审核通过后，`feature.images` 在保留旧数据的前提下追加新图片地址。
- 失败链路已跑通：故意发送 `FEATURE + bindEntityId=7`（该风采已逻辑删除），主队列只投递 1 次、不再无限重试，消息进入 `media.bind.dlq`，并且可从 `x-death` 头看到 `reason=rejected`。

这套链路现在已经具备两种能力：

- 成功消息：正常消费并回写业务表。
- 失败消息：从主队列移出，进入死信队列等待排查或补偿。

踩坑记录：

- 一开始 MQ 使用的是 Java 原生序列化，消费者报 `Attempt to deserialize unauthorized class com.backend.common.event.MediaBindMessage`。后面统一补上 `Jackson2JsonMessageConverter`，改成 JSON 消息后恢复正常。
- 用旧 JWT 测接口时会直接返回 403。本项目 token 带过期时间，联调前需要重新登录或临时生成一个新的测试 token。
- 第一次测 `FEATURE` 失败不是 MQ 配错，而是测试数据选错了：`feature.id=7` 已经逻辑删除，所以消费侧抛出“乡村风采不存在”。这也刚好证明了死信策略确实有必要。

## 五、Redis 缓存流程

项目里主要有三类缓存：

| 类型 | 作用 |
|------|------|
| 详情缓存 | 缓存单个资源详情 |
| 列表分页缓存 | 缓存分页查询结果 |
| 空值占位 | 防止不存在 ID 被反复打到数据库 |

详情缓存流程：

```text
查询详情
  -> 先查 null marker
  -> 查 Redis 详情缓存
  -> 命中则返回
  -> 未命中查数据库
  -> 数据不存在则写 null marker
  -> 数据存在则写详情缓存
```

列表缓存流程：

```text
查询列表
  -> 读取当前 list version
  -> 根据筛选条件生成 filter hash
  -> 拼出分页缓存 key
  -> 命中则返回
  -> 未命中查数据库
  -> 写入分页缓存
```

写操作失效流程：

```text
新增 / 修改 / 删除 / 审核
  -> 删除对应详情缓存
  -> bump list version
  -> 旧分页 key 不再被新请求命中
  -> 旧 key 等 TTL 自然过期
```

TTL 配置：

| 缓存类型 | 配置项 |
|----------|--------|
| 详情缓存 | `cache.ttl.detail-minutes` |
| 详情缓存随机抖动 | `cache.ttl.detail-jitter-seconds` |
| 列表缓存 | `cache.ttl.list-minutes` |
| 空值占位 | `cache.ttl.null-marker-minutes` |

缓存雪崩处理：

```text
详情缓存写入
  -> 使用基础 TTL
  -> 追加随机抖动秒数
  -> 把大量 key 的过期时间打散
  -> 避免同一时间批量回源数据库
```

当前实现里，详情缓存不是固定 TTL，而是：

```text
cache.ttl.detail-minutes + [0, cache.ttl.detail-jitter-seconds]
```

也就是“基础过期时间 + 随机抖动时间”。这样即使一批详情数据在接近的时间写入 Redis，它们的过期时刻也会被分散开，降低缓存雪崩概率。

关键工具：

- `SmartVillages-Backend/common/src/main/java/com/backend/common/utils/CacheKeyUtils.java`
- `SmartVillages-Backend/common/src/main/java/com/backend/common/utils/RedisJsonCacheTool.java`

面试讲法：

- 问题：列表分页缓存如果按条件和页码产生很多 key，更新时逐个删除很麻烦。
- 方案：缓存 key 里带版本号，写操作只递增版本号，让旧 key 自动失效。
- 为什么不用 `KEYS` 扫描删除：`KEYS` 在生产环境可能阻塞 Redis，版本号失效是 O(1) 写操作。
- 如何验证：先查列表产生缓存，再新增或审核数据，确认版本号递增，新请求命中新版本 key。

## 六、村务事项公开与后台缓存隔离

这次已经修过一个典型缓存问题：公开详情和后台详情不能共用同一个详情缓存 key。

问题场景：

```text
未发布村务事项真实存在
  -> 公开接口访问该 ID
  -> 因 status != 2 写入 null marker
  -> 后台详情也查同一个 key
  -> 后台误判为资源不存在
```

修复思路：

- 后台详情使用 `village_affair:admin:detail:{id}`。
- 公开详情使用 `village_affair:public:detail:{id}`。
- 更新、删除、审核时同时删除两类详情缓存。
- 公开详情命中缓存后直接返回，不再每次回源查库。

关键文件：

- `SmartVillages-Backend/management/src/main/java/com/backend/management/service/impl/VillageAffairServiceImpl.java`

面试讲法：

- 问题：同一个实体在公开端和后台端有不同可见性，不能共用空值占位。
- 方案：按访问场景拆分详情缓存 key。
- 为什么不是只判断 status：因为 null marker 本身也带业务语义，公开端的“不公开”不能等于后台端的“不存在”。
- 如何验证：公开访问未发布事项后，后台仍能正常查看该事项详情。

## 七、字段回显流程

数据库实体通常只存用户 ID，但前端需要显示用户名，所以 VO 要做字段回显。

通用流程：

```text
查询业务数据
  -> 转成 VO
  -> 收集 VO 中所有用户 ID
  -> 一次 IN 查询 auth 表
  -> 构建 id -> username 映射
  -> 回填到 VO 的 name 字段
```

设计重点：

- 列表回显用批量查询，避免 N+1。
- 详情回显可以复用批量方法，把单个 VO 包成集合。
- 缓存命中和未命中都要 enrich，保证用户名变更后能返回最新名称。

面试讲法：

- 问题：业务表只存用户 ID，但页面要展示用户名。
- 方案：返回前统一 enrich，批量收集 ID 后一次查询。
- 为什么不每条单独查：列表场景会出现 N+1 查询。
- 如何验证：修改用户名后再次查询列表或详情，名称能正确回显。

## 八、可观测性流程

TraceId 链路：

```text
请求进入
  -> 读取请求头 X-Trace-Id
  -> 没有则生成新的 UUID
  -> 写入 MDC
  -> 写入响应头 X-Trace-Id
  -> logback 通过 %X{traceId} 打印
  -> 请求结束清理 MDC
```

关键点：

- `TraceIdFilter` 要在 `JwtSecurityFilter` 之前。
- 响应头返回 `X-Trace-Id`，方便前端或接口测试工具定位问题。
- 日志中带 traceId，可以把同一次请求的日志串起来。

面试讲法：

- 问题：接口报错后，只看时间和接口路径很难定位完整链路。
- 方案：每个请求生成 traceId，贯穿响应头和日志。
- 为什么放在最前面的过滤器：后续认证和业务日志都需要同一个 traceId。
- 如何验证：请求接口后拿响应头 traceId，在日志里搜索同一个值。

## 九、统一异常与错误码流程

异常处理链路：

```text
业务代码抛出 BusinessException
  -> GlobalExceptionHandler 捕获
  -> 根据 ErrorCode 映射 HTTP 状态码
  -> 返回 Result<T>
```

常见映射：

| 场景 | HTTP 状态 |
|------|-----------|
| 无权限 | 403 |
| 资源不存在 | 404 |
| 限流 | 429 |
| 文件过大 | 413 |
| 参数或业务错误 | 400 |
| 未预期异常 | 500 |

面试讲法：

- 问题：不同 Controller 自己处理异常会导致返回格式不统一。
- 方案：统一业务异常和全局异常处理。
- 为什么要区分 HTTP 状态码：前端和调用方可以更准确判断错误类型。
- 如何验证：分别触发权限不足、资源不存在、限流、文件过大，检查状态码和响应结构。

## 十、分布式锁与缓存击穿保护

### 10.1 分布式锁

用 Redis SET NX + Lua 脚本实现了资源粒度的互斥锁，已接入核心写操作，避免同一资源被并发审核、上下架、删除或绑定媒体时出现覆盖写、重复处理和缓存失效顺序混乱。

抢锁：`SET lock:audit_media:{id} uuid NX EX 10`

- NX = key 不存在时才成功，原子操作
- EX 10 = 10 秒自动过期，防死锁

解锁：Lua 脚本

- GET key → 比较 value → 匹配才 DEL
- 三步原子执行，不解他人锁

**为什么解锁必须用 Lua：**

GET 和 DEL 是两个 Redis 命令，中间可能：

- A 持锁，执行 GET 通过
- 锁过期，B 抢到新锁
- A 继续执行 DEL，删掉了 B 的锁

Lua 脚本让"判断归属 + 删除"原子化，不会穿插。

**已接入的方法：**

| 模块 | 方法 | 锁 key |
|------|------|--------|
| media | `auditMedia` | `lock:audit_media:{id}` |
| announcement | `updateAnnouncement`、`updateStatus`、`deleteAnnouncement`、`auditAnnouncement`、`bindUploadedMedia` | `lock:announcement:*:{id}` / `lock:bindUpload:{id}` |
| feature | `updateStatus`、`updateFeature`、`deleteFeature`、`reviewFeature`、`bindUploadedMedia` | `lock:*:feature:{id}` / `lock:bindUpload:{id}` |
| interaction | `processingMessage`、`reviewMessage` | `lock:interaction:*:{id}` |
| villageAffair | `update`、`delete`、`audit` | `lock:villageAffair:*:{id}` |
| villageHouseLand | `update`、`delete` | `lock:villageHouseLand:*:{id}` |
| villageParty | `update`、`delete` | `lock:villageParty:*:{id}` |
| villagePopulation | `updateVillagePopulation`、`deleteVillagePopulation` | `lock:villagePopulation:*:{id}` |
| serviceTicket | `processingServiceTicket`、`doneServiceTicket` | `lock:village_service_ticket:*:{id}` |

### 10.2 缓存击穿保护

问题：详情缓存过期瞬间，几十个请求同时来查同一资源，全部穿透到数据库。

方案：在 `RedisJsonCacheTool` 新增 `getObject(key, type, Supplier<T> loader)` 重载。缓存未命中时只让一个请求拿到互斥锁去重建缓存，其他请求 sleep 50ms 后重读缓存。

```text
请求 A → miss → 抢锁成功 → loader.get() → 写缓存 → 放锁 → 返回
请求 B → miss → 抢锁失败 → sleep 50ms → 重读缓存 → hit → 返回
```

**已改造的详情查询：**

| 模块 | 方法 |
|------|------|
| admin | `getUserDetail` |
| announcement | `getAnnouncement`（含浏览量更新） |
| feature | `getFeatureDetail`（loader 内过滤未发布数据，命中后单独更新浏览量） |
| interaction | `getMessageDetail`、`getMyMessageDetail` |
| villageAffair | `getDetail`、`getPublicDetail` |
| villageHouseLand | `getVillageHouseLandDetail` |
| villageParty | `getDetail` |
| villagePopulation | `getVillagePopulationDetail` |
| serviceTicket | `getMyDetail`、`getServiceTicketDetail` |
| media | `getDetail` |

本轮顺手清理了详情缓存的旧手写逻辑：各详情查询统一走 loader 重载，空值占位由缓存工具托管；`interaction` 删除了遗留的 `tryLoadAndBumpFromCache`，`feature` 去掉了 loader 之后的二次 `getById`，避免未发布数据被写入详情缓存。

关键文件：

| 职责 | 文件 |
|------|------|
| 分布式锁工具 | `SmartVillages-Backend/common/src/main/java/com/backend/common/utils/RedisDistributedLock.java` |
| 缓存工具（含 loader 重载） | `SmartVillages-Backend/common/src/main/java/com/backend/common/utils/RedisJsonCacheTool.java` |
| 锁接入示例 | `SmartVillages-Backend/media/src/main/java/com/backend/media/service/impl/MediaServiceImpl.java` |
| loader 接入示例 | `SmartVillages-Backend/admin/src/main/java/com/backend/admin/service/impl/AdminServiceImpl.java` |

面试讲法：

- 问题：热门数据缓存过期时，大量请求同时穿透到数据库。
- 方案：用互斥锁控制缓存重建，只有一个请求去查库写缓存，其余等待并重读。
- 为什么不设永不过期：缓存数据有变更可能，永不过期会导致脏数据；用互斥锁既能保护数据库，又能保证数据最终一致。
- 如何验证：清掉某个热门资源的缓存后，用并发工具同时发几十个请求，看数据库日志只有一次查询。

### 10.3 缓存雪崩保护

问题：如果大量详情缓存使用完全相同的 TTL，并且在接近时间写入 Redis，它们可能在同一时间窗口集中失效，导致多类请求一起回源数据库，形成缓存雪崩。

方案：在 `RedisJsonCacheTool` 的默认详情缓存写入入口中加入 TTL 随机抖动。基础 TTL 由 `cache.ttl.detail-minutes` 控制，额外抖动范围由 `cache.ttl.detail-jitter-seconds` 控制，最终过期时间为“基础 TTL + 随机秒数”。

```text
写详情缓存
  -> 读取基础 TTL
  -> 生成 0 ~ jitterSeconds 的随机值
  -> 计算最终 TTL
  -> 写入 Redis
```

这样做的效果是：同一批详情缓存不会在同一秒一起失效，而是分散在一个时间窗口内陆续过期。它不能完全消灭并发回源，但能显著降低“很多 key 同时过期”的概率。

这里要和缓存击穿保护配合着看：

- 随机 TTL 负责把大量 key 的过期时间打散，降低雪崩概率。
- loader 互斥锁负责在单个 key 真的过期时，只让一个请求回源重建缓存。

两者不是替代关系，而是组合关系：前者解决“很多 key 同时过期”，后者解决“某个 key 过期后被并发打穿”。

关键文件：

| 职责 | 文件 |
|------|------|
| 缓存工具 | `SmartVillages-Backend/common/src/main/java/com/backend/common/utils/RedisJsonCacheTool.java` |
| 示例配置 | `SmartVillages-Backend/service/src/main/resources/application-example.yml` |

面试讲法：

- 问题：一批详情缓存如果用固定 TTL，可能在同一时间集中失效，数据库瞬间承压。
- 方案：给详情缓存 TTL 增加随机抖动，把同批 key 的过期时间打散。
- 为什么还需要互斥锁：随机 TTL 只能降低大量 key 同时过期的概率，单个热点 key 过期时仍然需要 loader 锁控制回源并发。
- 如何验证：观察 Redis 中同批详情 key 的剩余 TTL，会分布在一个时间区间而不是完全相同；同时清除热点 key 后并发访问，数据库仍只会出现一次回源查询。

## 十一、AOP 操作日志

这次在公共模块补了一个很适合项目继续扩展的 AOP 能力：对管理端关键写操作统一记录日志。

问题场景：

```text
公告更新、村务审核、工单处理这些操作分散在不同 ServiceImpl
  -> 每个方法如果手写耗时日志和操作人日志
  -> 代码会重复
  -> 业务方法容易掺杂很多非核心逻辑
  -> 后续新增模块时也容易漏记日志
```

方案：

- 在 `common` 模块新增 `OperationLogAspect`
- 新增 `@OperationLog` 自定义注解，显式标注需要记录日志的方法
- 用 `@Around("@annotation(operationLog)")` 拦截带注解的方法
- 统一记录当前操作人 `authId`
- 统一记录注解里定义的业务动作名 `action`
- 统一记录类名、方法名、执行耗时
- 成功和失败都打印日志，失败时保留异常堆栈

当前已接入的典型方法包括：

- `announcement`：`updateAnnouncement`、`updateStatus`、`auditAnnouncement`
- `feature`：`updateStatus`、`updateFeature`、`deleteFeature`、`reviewFeature`
- `interaction`：`processingMessage`、`reviewMessage`
- `management`：`VillageAffairServiceImpl.audit`、`VillageServiceTicketServiceImpl.processingServiceTicket`、`VillageServiceTicketServiceImpl.doneServiceTicket`、`VillagePartyServiceImpl.update`、`VillagePartyServiceImpl.delete`、`VillagePopulationServiceImpl.updateVillagePopulation`、`VillagePopulationServiceImpl.deleteVillagePopulation`

执行流程：

```text
请求进入写操作方法
  -> AOP 命中 @OperationLog 注解
  -> AOP 切面先拿到类名、方法名、开始时间
  -> 从注解中读取 action 业务动作名
  -> 通过 RequestContextHolder 取当前 HttpServletRequest
  -> 通过 LoginUserContext 读取 authId
  -> 放行业务方法执行
  -> 成功则记录 success 日志 + 耗时
  -> 异常则记录 failed 日志 + 耗时 + 异常栈
```

为什么用 AOP：

- 操作日志属于横切逻辑，不应该散落在每个业务方法里重复写。
- AOP 可以在不侵入业务代码的前提下，统一增强关键操作。
- 用自定义注解显式声明哪些方法要记日志，比按 `update*`、`delete*` 这类方法名规则硬匹配更清晰，也更方便后续精细控制。
- 当前这版先做日志输出，后续如果要升级成操作审计表、参数摘要记录、按角色区分日志策略，都可以继续沿着这个切面扩展。

关键文件：

| 职责 | 文件 |
|------|------|
| AOP 注解 | `SmartVillages-Backend/common/src/main/java/com/backend/common/aop/OperationLog.java` |
| AOP 切面 | `SmartVillages-Backend/common/src/main/java/com/backend/common/aop/OperationLogAspect.java` |
| 登录用户上下文 | `SmartVillages-Backend/common/src/main/java/com/backend/common/context/LoginUserContext.java` |
| AOP 依赖 | `SmartVillages-Backend/common/pom.xml` |

面试讲法：

- 问题：管理端关键写操作分布在多个模块，如果每个方法都手写日志和耗时统计，代码会重复且难维护。
- 方案：基于 Spring AOP + 自定义 `@OperationLog` 注解编写统一切面，记录操作人、业务动作、目标类、方法名和执行耗时。
- 为什么不用在每个方法里直接写日志：那样会污染业务代码，也容易因为遗漏导致日志口径不一致；用注解还能显式声明哪些方法需要纳入日志范围。
- 如何验证：调用公告更新/审核、乡村风采更新/审核、留言处理/审核、村务审核、工单处理/办结、党建组织更新/删除、人口台账更新/删除等接口后，查看日志中是否输出 `authId`、`action`、类名、方法名和耗时；故意触发业务异常时，检查失败日志和异常栈是否完整。

## 十二、单体到微服务的渐进式拆分

本阶段将原 Spring Boot 多模块单体项目，按运行边界拆出 `gateway-service`、`auth-service`、`business-service`、`media-service` 和 `common-lib`，先完成服务壳子、路由入口、依赖收口和跨服务调用改造。

### 12.1 服务边界

| 服务 | 端口 | 职责 |
|------|------|------|
| `gateway-service` | 8080 | 统一入口，按路径转发到后端服务 |
| `auth-service` | 8082 | 登录认证、用户管理、管理员管理 |
| `business-service` | 8081 | 公告、乡村风采、留言互动、村务管理 |
| `media-service` | 8083 | 媒体上传、审核、媒体绑定消息发送 |
| `common-lib` | — | 跨服务共享契约，如 MQ 消息、Binder 接口 |

### 12.2 网关路由

当前 Gateway 路由：

| 路径 | 目标服务 | 说明 |
|------|----------|------|
| `/auth/**` | `auth-service` | 登录、退出 |
| `/admin/features/**` | `business-service` | 风采审核属于业务服务 |
| `/admin/**` | `auth-service` | 用户、干部管理 |
| `/media/**` | `media-service` | 媒体上传、审核、查询 |
| `/api/**` | `business-service` | 业务接口统一入口，使用 `StripPrefix=1` 去掉 `/api` 前缀 |

`/admin/features/**` 必须放在 `/admin/**` 前面，因为它更具体，否则会被 `/admin/**` 提前匹配到 `auth-service`。

### 12.3 用户名查询的跨服务调用

原来 `media-service` 和 `business-service` 会直接依赖 `auth` 模块，通过 `AuthMapper` 查询用户表。

改造后统一抽象为：

```text
业务代码 -> AuthUserQueryService -> RemoteAuthUserQueryService -> FeignClient -> auth-service
```

`auth-service` 提供内部接口：

```text
GET /internal/auth/usernames?ids=1&ids=2
```

返回用户 ID 到用户名的映射。

`business-service` 和 `media-service` 各自维护自己的 `AuthUserClient`：

| 服务 | Feign Client |
|------|--------------|
| `business-service` | `com.backend.business.client.AuthUserClient` |
| `media-service` | `com.backend.mediaservice.client.AuthUserClient` |

这里没有把 `business-service` 的 `AuthUserClient` 直接给 `media-service` 复用，因为这样会变成 `media-service` 依赖 `business-service`，服务边界又被粘回去。Feign Client 是每个服务自己的出站接口，只描述“我要怎么调用 auth-service”，不应该从另一个业务服务模块借。

这样做的目的：

- `auth` 表只由 `auth-service` 管理；
- `business-service` 和 `media-service` 不再直接依赖 `auth` 模块；
- 业务代码只依赖 `AuthUserQueryService` 抽象，不关心底层是本地查询、RestTemplate 还是 Feign；
- 后续如果接入 Nacos 服务发现，只需要把 Feign 的 `url` 写死方式改成服务名调用，业务代码不用改。

关键点：

- `@FeignClient` 标记一个远程 HTTP 调用接口。
- `@EnableFeignClients` 告诉 Spring 去指定包扫描 Feign 接口，并生成代理 Bean。
- `RemoteAuthUserQueryService` 注入 `AuthUserClient`，负责把远程调用结果转换成业务需要的 `Map<Integer, String>`。
- `services.auth.url` 暂时配置为 `http://localhost:8082`，表示 auth-service 当前地址。

### 12.4 依赖清理

已清理的直接依赖：

| 模块 | 清理内容 |
|------|----------|
| `announcement` | 移除 `auth` 依赖 |
| `feature` | 移除 `auth`、`media` 依赖 |
| `interaction` | 移除 `auth` 依赖 |
| `management` | 移除 `auth` 依赖 |
| `business-service` | 移除 `auth` 依赖和 `auth.mapper` 扫描 |
| `media-service` | 移除 `auth` 依赖和 `auth.mapper` 扫描 |
| `admin` | 移除 `announcement`、`media` 依赖，保留 `auth` 依赖 |

### 12.5 当前阶段说明

当前还不是完整生产级微服务，属于渐进式拆分阶段：

- 服务地址暂时写死为 `localhost:端口`；
- 远程调用已从 `RestTemplate` 逐步改为 OpenFeign；
- 暂未接入注册中心、配置中心、熔断降级；
- 数据库仍然共用同一个库，后续可继续按服务拆库。

已验证链路：

```text
gateway-service -> business-service -> auth-service
```

通过 `/api/announcements` 和 `/api/features` 验证，返回数据中已能正常带出 `createUserName`、`auditUserName` 等用户名称字段。

### 12.6 面试讲法

- 问题：原项目虽然是 Maven 多模块，但运行上仍是单体，不同业务模块容易直接依赖彼此代码和数据库表。
- 方案：先拆出 Gateway、认证服务、业务服务和媒体服务，用网关统一入口；再通过 `AuthUserQueryService` 抽象和 OpenFeign，把业务服务对用户表的直接访问改为调用 `auth-service`。
- 为什么渐进式拆：一次性拆库、注册中心、Feign、熔断全上风险太高；先拆运行边界和依赖边界，更容易验证。
- 为什么每个服务各写一个 Feign Client：Feign Client 是服务自己的出站调用接口，复用另一个业务服务的 Client 会造成服务之间的代码依赖。
- 工程价值：降低模块耦合，让服务职责更清楚，也为后续 Nacos、配置中心、熔断降级和独立部署打基础。

## 十三、后续功能记录规则

以后每完成一个有完整生命周期的业务功能，都要记录到项目回顾中。

记录格式：

```text
功能名称：
业务问题：
完整生命周期：
核心设计：
工程价值：
为什么不用更简单但更差的办法：
如何验证：
简历表达：
面试讲法：
```

当前已记录的完整功能：

| 功能 | 状态 | 可讲亮点 |
|------|------|----------|
| 媒体上传、审核、绑定 | 已完成 | 事件驱动 + Binder 策略分发 + 模块解耦 |
| Redis 详情和列表缓存 | 已推进 | 空值占位 + TTL 分层 + 版本号批量失效 |
| 登录限流 | 已实现 | Redis 计数 + 429 错误码 |
| TraceId 链路追踪 | 已实现 | 请求头、响应头、MDC、日志串联 |
| 分布式锁（媒体审核） | 已接入 | SET NX + Lua 脚本原子解锁 |
| 缓存击穿保护 | 已接入核心详情查询 | 互斥锁重建 + sleep 重读 |
| 缓存雪崩保护 | 已接入详情缓存 | 基础 TTL + 随机抖动 |
| AOP 操作日志 | 已接入管理端关键写操作 | 自定义注解 + 统一耗时/操作人日志 |
| RabbitMQ 异步媒体绑定 | 已接入 | MQ 解耦 + Binder 策略分发 + 消费侧幂等 + 死信队列 |
| 单体到微服务渐进式拆分 | 已推进 | Gateway 路由 + 服务边界拆分 + 远程调用替代直接依赖 |

## 十四、每周推进节奏

原则：每天约 2 小时，不贪多，每周沉淀一个能演示、能写简历、能讲清取舍的亮点。

建议顺序：

1. 先把已有功能讲清楚：媒体绑定、缓存、限流、TraceId。
2. 再补验证材料：接口截图、日志截图、Redis key 示例。
3. 再选下一个亮点：留言限流、上传限流、统一响应带 traceId、异步消息、MinIO。
4. 每做完一个闭环功能，就补到本文件。

## 十五、简历表达草稿

媒体资源绑定：

> 设计并实现媒体上传后的审核绑定流程，基于 RabbitMQ 和 Binder 策略分发将审核主流程与公告、乡村风采、用户头像等业务绑定逻辑解耦；新增绑定目标只需扩展 Binder 实现，并针对风采相册追加场景补充消费侧幂等处理、针对业务异常补充死信队列策略，避免重复消费和无效消息无限重试。

Redis 缓存：

> 为公告、媒体、村务等核心查询引入 Redis 详情缓存和分页列表缓存，使用空值占位降低缓存穿透风险，并通过版本号批量失效列表缓存，避免生产环境使用 `KEYS` 扫描删除。

缓存稳定性增强：

> 在 Redis 详情缓存中落地空值占位、互斥锁重建和 TTL 随机抖动，分别应对缓存穿透、缓存击穿和缓存雪崩，降低高并发场景下数据库瞬时压力。

AOP 操作日志：

> 在公共模块中基于 Spring AOP 和自定义注解实现管理端关键写操作日志切面，统一记录操作人、业务动作、目标方法和执行耗时，减少重复日志代码并为后续操作审计扩展预留入口。

登录限流：

> 在登录流程中接入 Redis 限流，按 IP 与用户名组合计数，对异常登录频率返回 429，降低暴力尝试风险。

TraceId：

> 实现请求级 TraceId 追踪，将 `X-Trace-Id` 贯穿请求头、响应头、MDC 和日志输出，提升接口问题定位效率。
